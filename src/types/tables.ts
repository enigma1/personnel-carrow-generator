export type ActiveKeeperAssociations = {
  code: string;
  name: string;
  era1: string;
  era2: string;
  era3: string;
};

export type DuplicateKeepers = {
  code: string;
  name: string;
  indices: number[];
};
